-- Sample tables for practicing SQL subqueries
-- DROP TABLE IF EXISTS employees;
-- DROP TABLE IF EXISTS departments;
-- CREATE TABLE departments (
--     id SERIAL PRIMARY KEY,
--     name VARCHAR(50) NOT NULL UNIQUE
-- );
-- CREATE TABLE employees (
--     id SERIAL PRIMARY KEY,
--     name VARCHAR(100) NOT NULL,
--     department_id INT REFERENCES departments(id),
--     salary INT NOT NULL,
--     hire_date DATE NOT NULL
-- );
-- INSERT INTO departments (name) VALUES
--     ('Engineering'),
--     ('Sales'),
--     ('Marketing'),
--     ('HR');
-- INSERT INTO employees (name, department_id, salary, hire_date) VALUES
--     ('Alice', 1, 90000, '2023-01-10'),
--     ('Bob', 1, 85000, '2022-06-15'),
--     ('Charlie', 2, 70000, '2021-11-20'),
--     ('Diana', 2, 72000, '2023-03-05'),
--     ('Ethan', 3, 65000, '2020-09-12'),
--     ('Fiona', 3, 68000, '2024-02-01'),
--     ('George', 4, 60000, '2022-07-22');
-- Example subquery practice:
-- SELECT name,
--     salary
-- FROM employees
-- WHERE department_id IN (
--         SELECT id
--         FROM departments
--         WHERE name IN ('Engineering', 'Sales')
--     );
-- Another example:
-- SELECT name,
--     salary
-- FROM employees
-- WHERE salary > (
--         SELECT AVG(salary)
--         FROM employees
--     );
-- SELECT AVG(salary) AS average_salary
-- FROM employees;